overwrite: true
schema: "./generator/schema.gql"
documents: null
generates:
  ./generator/FabricGQLTypes.ts:
    plugins:
      - "typescript"
  ./generator/graphql.schema.json:
    plugins:
      - "introspection"
