overwrite: true
schema: "./generator/schema.gql"
documents: null
generates:
  ./generator/ultraGQLTypes.ts:
    plugins:
      - "typescript"
  ./generator/graphql.schema.json:
    plugins:
      - "introspection"
